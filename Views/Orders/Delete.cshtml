@* @model DATN.Models.Order
@{
    ViewData["Title"] = "Xoá đơn hàng";
    // Layout = "~/Views/Admin/_Layout.cshtml";
}

<h2>Xoá đơn hàng</h2>

<p>Bạn có chắc chắn muốn xoá đơn hàng <strong>#@Model.OrderID</strong> không?</p>

<p>
    <strong>Người nhận:</strong> @Model.RecipientName<br />
    <strong>SĐT:</strong> @Model.RecipientPhone<br />
    <strong>Địa chỉ:</strong> @Model.DeliveryAddress
</p>

<form asp-action="Delete">
    <input type="hidden" asp-for="OrderID" />
    <button type="submit" class="btn btn-danger">Xoá</button>
    <a asp-action="Index" class="btn btn-secondary">Huỷ</a>
</form>
 *@